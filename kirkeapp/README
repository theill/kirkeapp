= Build

# merge from master into church specific branch (maintenance)

$ git checkout kokkedal
$ git diff kokkedal master
$ git merge master

# merge part of church specific branch into master

$ git checkout master
$ git diff master kokkedal
$ git merge kokkedal


= Build new maintenance release

$ git checkout kokkedal
$ build
$ cp kokkedal.ipa /ready-to-deploy


= Church specific files

* kirkeapp/Icon-72.png
* kirkeapp/Icon.png
* kirkeapp/Icon@2x.png
* kirkeapp/Images/churches/frontpage@2x.png
* kirkeapp/Info.plist
* kirkeapp/Settings/App.plist
